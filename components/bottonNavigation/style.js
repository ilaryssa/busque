import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  bottomNavigation: {
    textAlign: 'center',
    fontSize: 20,
    marginTop: 20,
  },
  container: {
    flex:1,
    width: '100%',
  },
  title: {
    textAlign: 'center',
    fontSize: 30,
    marginTop: 50,
    marginBottom: 20,
  },
});

export default styles;